<h1>All Students</h1>

<% if current_user.role == "admin" %>
  <%= link_to "Add New Student", new_student_path, class: "btn btn-primary" %>
<% end %>

<table>
  <thead>
    <tr>
      <th>Student Number</th>
      <th>Course</th>
      <th>Name</th>
      <th>Email</th>
      <% if current_user.role == "admin" %>
        <th>Actions</th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @students.each do |student| %>
      <tr>
        <td><%= student.student_number %></td>
        <td><%= student.course %></td>
        <td><%= student.name %></td>
        <td><%= student.email %></td>
        <% if current_user.role == "admin" %>
          <td>
            <%= link_to "Edit", edit_student_path(student) %> |
            <%= button_to "Delete", student_path(student), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger" %>
          </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
